<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/cards.css"/>
    <link rel="stylesheet" href="/stylesheets/global.css"/>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
      integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T"
      crossorigin="anonymous"
    />
    <link href="https://unpkg.com/material-components-web@latest/dist/material-components-web.min.css" rel="stylesheet">
    <script src="https://unpkg.com/material-components-web@latest/dist/material-components-web.min.js"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
  </head>
  <body>
    <%- include('navbar') -%>
    <div id="content">
      <% if (trips.length == 0) { %>
        <h1 id="nothing">None</h1>
      <% } %>
      <% for (var i = 0; i < trips.length; i++) { %>
        <div class="">
          <div class="label"><b>Name:</b> <%= trips[i].name %></div>
          <br>
          <div class="label"><b>Flight:</b></div>
          <div class="label">Flight Code: <%= flights[i].flight_code %></div>
          <div class="label">Date: <%= flights[i].date %></div>
          <div class="label">Departing From: <%= flights[i].departing_airport %></div>
          <div class="label">Arriving At: <%= flights[i].arriving_airport %></div>
          <div class="label">Departure Time: <%= flights[i].departure_time %></div>
          <div class="label">Arrival Time: <%= flights[i].arrival_time %></div>
          <br>
          <div class="label"><b>Stay:</b></div>
          <div class="label">Name of Accommodation: <%= stays[i].name %></div>
          <div class="label">Address: <%= stays[i].address %></div>
          <div class="label">Check-In Date: <%= stays[i].checkin_date %></div>
          <div class="label">Check-Out Date: <%= stays[i].checkout_date %></div>
          <div class="label">Check-In Time: <%= stays[i].checkin_time %></div>
          <div class="label">Check-Out Time: <%= stays[i].checkout_time %></div>
          <form id='delete' action='/trips/delete' method="POST">
            <input type='hidden' value=<%= trips[i]._id %> name='trip_id'>
            <input type='submit' value="Delete">
          </form>
          <hr>
        </div>
      <% } %>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  </body>
</html>
